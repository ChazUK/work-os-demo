verbose: true

pre-commit:
  parallel: true
  jobs:
    - name: format
      glob: '**/*.{html,css,json,md,scss,svg,js,ts,tsx,mjs,cjs,mts,xml,yml,yaml}'
      run: pnpm exec prettier --write {staged_files}
      stage_fixed: true
